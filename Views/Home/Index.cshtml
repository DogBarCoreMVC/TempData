@model IEnumerable<TempData.Models.Product>

@{
    ViewData["Title"] = "Index";
    var ProductList = (IEnumerable<Product>)TempData["ProductList"];
    //1.ข้อมูลสินค้ายังคงอยู่ในรูปแบบเป็นชุด @model IEnumerable<TempData.Models.Product>
    //2.แปลงข้อมูลที่อยู่ในคลาส TempData ให้เก็บอยู่ในตัวแปรที่ชื่อว่า var ProductList = (IEnumerable<Product>)TempData["ProductList"]
    //3.วนลูป foreach อ่านข้อมูลสินค้าแต่ล่ะคนที่เก็บอยู่ในตัวแปร ProductList @foreach (var item in ProductList)

}

<h1>Index</h1>

<p>
    <a asp-action="Create"></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in ProductList) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
